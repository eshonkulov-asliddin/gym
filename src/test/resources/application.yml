#set active profile
spring:
  datasource:
    driver-class-name: org.testcontainers.jdbc.ContainerDatabaseDriver
    url: jdbc:tc:mysql:8.2:///gym_test_db

  jpa:
    hibernate:
      ddl-auto: create